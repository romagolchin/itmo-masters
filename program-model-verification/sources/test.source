method main ()
begin
    f(-10);
    a---b;
    !!!!f(x+-y)&&!(1+-2=3) || zzz && qqq;
    -(b+c*d);
    c := 12 - d[-42 -Y(z), e * (98 / f)] + -1;
    a:=true;
    h := 'x';
    while true do begin
        print("hello");
        if tired then break;
    end;
    if k(t + 0xa) then
        begin
            'x';
            z := 3 + "abc";
            if true then true;
        end;
        else 'y';
    x := f('y', '\'', "z\"zz\"z", 1+2);
    "\"\"\"";
    "\"a\"\"";
end;

class C
    var
        a,b: int;
        x, y: char;
begin
    method getA(c: int): int
    begin
        new B().b(x, yz(b, a).zzz(1));
        a;
    end;
end

method gcd(a: int, b: int)
begin
    if a = 0
        then b;
        else
            begin
                if b = 0
                    then a;
                    else gcd(b, b % a);
            end;

    writeln("hw!");
end;

method f(z: byte)
    var
        a,b: int;
        x, y: char;
begin
    print(x, b);
    a := z * z;
    a;
end;
